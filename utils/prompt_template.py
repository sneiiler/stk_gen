"""
Prompt templates for satellite cluster optimization.

This module contains versioned prompt templates for the satellite cluster
optimization system, specifically for dynamic satellite cluster partitioning.
"""

from pydantic import BaseModel, Field
from typing import List
from data_classes.sft_data_models import RawConstellationDataModel, SatelliteClusterOutput

# Version 1.0 - Initial satellite cluster optimization prompt
SATELLITE_CLUSTER_V1 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用python工具进行计算，依靠纯粹逻辑推理。你是一位专攻复杂优化与图论的大模型专家，负责动态卫星集群的划分。在给定卫星属性、星间链路与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）

1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和。
2. 最大化观测质量：增大簇所覆盖目标的 target_edges.q 之和。
3. 优先使用健康卫星：优先选取 sat_attrs.health 较高的卫星。

二、决策逻辑与约束（Constraints & Logic）

1. 主节点（Master）选择

    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
2. 分簇策略（strategy）

    - **"balanced"**：

      1. 识别所有可被观测的目标。
      2. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      3. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。此策略下，不要求所有卫星都参与组簇，保证资源有效利用。
    - **"quality"**：

      1. 按上述"balanced"生成初始簇；
      2. 进行**簇扩展**：寻找那些当前未被分配、健康度良好、且与簇内已有成员（尤其是主节点）具有高链路强度`w`的卫星，将它们吸纳进簇作为成员星。这旨在为未来的观测任务或链路中继做好准备。
    - 分簇校验：

      **尽可能少的分簇**，判断如果不同的簇之间的卫星如果存在链接关系，并且评估之后链接质量可以的情况下，应该合并不同的簇
3. 校验

    - 输出的 targets 集合必须与输入 target_edges 完全一致，无新增、无遗漏。 禁止幻觉。
    - "balanced" 模式下，每簇卫星数 ≤ 目标数；"quality" 模式下，每簇卫星数 ≤ 2×目标数。

三、输入格式
```json
{{
  "timestamp": "ISO8601 字符串",
  "strategy": "balanced" 或 "quality",
  "sat_attrs": [
    {{ "id": 整数, "health": 0–10, "pos": [浮点, 浮点, 浮点] }}
  ],
  "sat_edges": [
    {{ "from": 卫星ID, "to": 卫星ID, "w": 0–1 }}
  ],
  "target_edges": [
    {{ "from": 卫星ID, "to": 目标ID, "q": 0–1 }}
  ]
}}
```

四、输出格式

你的输出必须严格遵守以下JSON结构，并包含一个详细的思考过程。

- 思维链: 你需要完整的把你的思考过程放在标签<chain_of_thought>完整的思考过程在这里...</chain_of_thought>。尽可能详尽的描述，并确保正确，不要幻觉，不要遗漏。
- **结果**:紧跟在思维链后面，在这个部分，提供最终的JSON结果数组，你不要有额外的字符了。
- <|EOF|>: 输出截止符号

以上的输出格式必须严格遵守，否则视为无效输出，输出格式这个要求不要出现在思维链中

```json
<chain_of_thought>
// 详尽的思考链，逐步说明如何选主节点、分簇与校验……
</chain_of_thought>
[
  {{
    "cluster_id": 整数,
    "master": 卫星ID,
    "sats": [卫星ID, …],
    "targets": [目标ID, …]
  }}
]
<|EOF|>
```
"""

# Version 2.0 - Enhanced satellite cluster optimization prompt with improved constraints
SATELLITE_CLUSTER_V2 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用python工具进行计算，依靠纯粹逻辑推理。你是一位专攻复杂优化与图论的大模型专家，负责动态卫星集群的划分。在给定卫星属性、星间链路与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）

1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和。
2. 最大化观测质量：增大簇所覆盖目标的 target_edges.q 之和。
3. 优先使用健康卫星：优先选取 sat_attrs.health 较高的卫星。
4. 最小化簇间干扰：减少不同簇之间的资源竞争。

二、决策逻辑与约束（Constraints & Logic）

1. 主节点（Master）选择

    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
    - 主节点应具有较高的观测能力（与目标的平均q值较高）。

2. 分簇策略（strategy）

    - **"balanced"**：

      1. 识别所有可被观测的目标。
      2. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      3. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。此策略下，不要求所有卫星都参与组簇，保证资源有效利用。
      4. 优先考虑观测质量，其次考虑链路强度。

    - **"quality"**：

      1. 按上述"balanced"生成初始簇；
      2. 进行**簇扩展**：寻找那些当前未被分配、健康度良好、且与簇内已有成员（尤其是主节点）具有高链路强度`w`的卫星，将它们吸纳进簇作为成员星。这旨在为未来的观测任务或链路中继做好准备。
      3. 扩展时优先考虑链路强度，确保簇内通信质量。

    - 分簇校验：

      **尽可能少的分簇**，判断如果不同的簇之间的卫星如果存在链接关系，并且评估之后链接质量可以的情况下，应该合并不同的簇。

3. 校验

    - 输出的 targets 集合必须与输入 target_edges 完全一致，无新增、无遗漏。 禁止幻觉。
    - "balanced" 模式下，每簇卫星数 ≤ 目标数；"quality" 模式下，每簇卫星数 ≤ 2×目标数。
    - 每个卫星最多只能属于一个簇。
    - 每个簇必须至少包含一个卫星和一个目标。

三、输入格式
```json
{{
  "timestamp": "ISO8601 字符串",
  "strategy": "balanced" 或 "quality",
  "sat_attrs": [
    {{ "id": 整数, "health": 0–10, "pos": [浮点, 浮点, 浮点] }}
  ],
  "sat_edges": [
    {{ "from": 卫星ID, "to": 卫星ID, "w": 0–1 }}
  ],
  "target_edges": [
    {{ "from": 卫星ID, "to": 目标ID, "q": 0–1 }}
  ]
}}
```

四、输出格式

你的输出必须严格遵守以下JSON结构，并包含一个详细的思考过程。

- 思维链: 你需要完整的把你的思考过程放在标签<chain_of_thought>完整的思考过程在这里...</chain_of_thought>。尽可能详尽的描述，并确保正确，不要幻觉，不要遗漏。
- **结果**:紧跟在思维链后面，在这个部分，提供最终的JSON结果数组，你不要有额外的字符了。
- <|EOF|>: 输出截止符号

以上的输出格式必须严格遵守，否则视为无效输出，输出格式这个要求不要出现在思维链中

```json
<chain_of_thought>
// 详尽的思考链，逐步说明如何选主节点、分簇与校验……
</chain_of_thought>
[
  {{
    "cluster_id": 整数,
    "master": 卫星ID,
    "sats": [卫星ID, …],
    "targets": [目标ID, …]
  }}
]
<|EOF|>
```
"""

# Version 3.0 - Advanced satellite cluster optimization with fault tolerance
SATELLITE_CLUSTER_V3 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用任何工具进行计算，依靠纯粹逻辑推理。你的任务是专攻复杂优化与图论的解决，负责动态大规模星座卫星集群的划分。在给定卫星属性、星间链接情况与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）
1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和。
2. 最大化观测质量：增大簇所覆盖目标的 target_edges.q 之和。
3. 优先使用健康卫星：优先选取 sat_attrs.health 较高的卫星。
4. 最小化簇间干扰：同一个卫星不可以出现在两个不同的簇内。
5. 最小化原则：在满足约束条件的情况下，尽可能少的分簇。

二、决策逻辑与约束（Constraints & Logic）

1. 主节点（Master）选择

    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
    - 主节点应具有较高的观测能力（与目标的平均q值较高）。
    - 主节点应具有良好的链路连接性，能够有效协调簇内成员。

2. 分簇策略（strategy）

    - **"balanced"**：

      1. 识别所有可被观测的目标。
      2. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      3. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。此策略下，不要求所有卫星都参与组簇，保证资源有效利用。
      4. 优先考虑观测质量，其次考虑链路强度。
      5. 任意卫星到主节点最大连接跳数小于等于2。

    - **"quality"**：

      1. 按上述"balanced"生成初始簇；
      2. 进行**簇扩展**：寻找那些当前未被分配、健康度良好、且与簇内已有成员（尤其是主节点）具有高链路强度`w`的卫星，将它们吸纳进簇作为成员星。这旨在为未来的观测任务或链路中继做好准备。
      3. 扩展时优先考虑链路强度，确保簇内通信质量。
      4. 考虑卫星的冗余配置，提高系统容错能力。
      5. 任意卫星到主节点最大连接跳数小于等于3。

    - 分簇校验：

      **尽可能少的分簇**，判断如果不同的簇之间的卫星如果存在链接关系，并且评估之后链接质量可以的情况下，应该合并不同的簇。
      - 检查簇间是否存在强链路连接，如果存在且合并后不会显著降低观测质量，则考虑合并。
      - 确保合并后的簇仍然满足约束条件。

3. 校验

    - 禁止遗漏目标，禁止遗漏目标，这是最重要的原则：输出的 targets 集合必须与输入 target_edges 完全一致，无新增、禁止幻觉。
    - 检查每个分簇卫星数量合规："balanced" 模式下，每簇卫星数 ≤ 目标数；"quality" 模式下，每簇卫星数 ≤ 2×目标数。
    - 检查卫星任务冲突：每个卫星最多只能属于一个簇。
    - 检查卫星最大连接跳数：同一个簇内不允许出现孤星，即任意两颗卫星到主节点最大连接跳数小于等于3。

三、输入格式
```json
{input_instructions}
```
四、输出格式

你的输出必须严格遵守以下JSON结构

- 思维链: 将你**详细的思考过程**放在`chain_of_thought`标签内。即：禁止分析思考结果，而是要原样输出思考结果。尽可能详尽的描述，挨个分析卫星和目标之间的链接关系，并确保正确，不要幻觉，不要遗漏。
- 结果: 紧跟在思维链后面，在这个部分，提供最终的JSON结果数组，你不要有额外的字符了。
{output_format_instructions}
以上的输出格式必须严格遵守，你应该始终使用中文进行推理和输出，禁止使用英文，否则视为无效输出，输出格式这个要求不要出现在思维链中。

**注意控制你的思考时间，你的时间有限，必须快速决策**
"""


# Version 4.0 - Advanced satellite cluster optimization with fault tolerance
SATELLITE_CLUSTER_V4 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用任何工具进行计算，依靠纯粹逻辑推理。你的任务是专攻复杂优化与图论的解决，负责动态大规模星座卫星集群的划分。在给定卫星属性、星间链接情况与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）
1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和（权重权值 0.4）。
2. 最大化观测质量：增大簇所覆盖目标的 target_edges.q 之和（权重权值 0.3）。
3. 最小化簇数：在满足约束条件下，减少簇的数量（权重权值 0.2）。
4. 优先使用健康卫星：优先选取 health 高的卫星（权重权值 0.1）。

二、决策逻辑与约束（Constraints & Logic）

```
定义全局变量：
  cluster_merge_threshold: 0.7
  balanced_mode_connection_jump: 2
  quality_mode_connection_jump: 3

```

1. 主节点（Master）选择

    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
    - 主节点应具有良好的链路连接性，能够有效协调簇内成员。

2. 分簇策略（strategy）

    - **"balanced"**：
      1. 识别所有可被观测的目标。
      2. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      3. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。此策略下，不要求所有卫星都参与组簇，每簇卫星数 ≤ 目标数，节约卫星资源。
      4. 优先考虑观测质量，其次考虑链路强度。
      5. 任意卫星到主节点最大连接跳数 ≤ balanced_mode_connection_jump，此时max_connection_jump = balanced_mode_connection_jump。
      6. 对所有簇对按降序扫描跨簇链接 w；  
      7. 若 w ≥ cluster_merge_threshold，且合并后任意卫星到新主节点跳数 ≤ balanced_mode_connection_jump ，执行合并；  
      8. 重复步骤 6-7 直到无可合并簇对。

    - **"quality"**：

      1. 按上述"balanced"生成初始簇；
      2. 进行**簇扩展**：寻找那些当前未被分配、健康度良好、且与簇内已有成员（尤其是主节点）具有高链路强度`w`的卫星，将它们吸纳进簇作为成员星。这旨在为未来的观测任务或链路中继做好准备。
      3. 扩展时优先考虑链路强度，确保簇内通信质量。
      4. 考虑卫星的冗余配置，提高系统容错能力。
      5. 任意卫星到主节点最大连接跳数 ≤ quality_mode_connection_jump。此时max_connection_jump =  quality_mode_connection_jump

    - **簇合并**规则：
      1. 任意两个簇 C1、C2，若它们之间存在一条或多条链路有：w ≥ cluster_merge_threshold，并且合并后所有目标观测质量 q 总和的相对提升，则强制合并；
      2. 优先合并总 w 最大的簇对，一次处理一个合并操作，再迭代。

3. 校验

    - 禁止遗漏目标，禁止遗漏目标，这是最重要的原则：输出的 targets 集合必须与输入 target_edges 完全一致，无新增、禁止幻觉，否则视为无效输出。
    - 检查每个分簇卫星数量合规："balanced" 模式下，每簇卫星数 ≤ 目标数；"quality" 模式下，每簇卫星数 ≤ 2×目标数。
    - 检查卫星任务冲突：每个卫星最多只能属于一个簇。
    - 检查卫星最大连接跳数：同一个簇内不允许出现孤星，即任意卫星到主节点最大连接跳数 ≤ max_connection_jump 。

三、输入格式
```json
{input_instructions}
```
四、输出格式

你的输出必须严格遵守以下JSON结构
{output_format_instructions}

输出要求：
- 思维链: 将你**详细的思考过程**放在`chain_of_thought`标签内。即：禁止偷懒总结概括，而是要原样输出思考结果。尽可能详尽的描述，轮次分析卫星和目标之间的链接关系，并确保正确，不要幻觉，不要遗漏。
- 结果: 紧跟在思维链后面，在这个部分，提供最终的JSON结果数组。

以上的输出格式必须严格遵守，你应该始终使用中文进行推理和输出，禁止使用英文，否则视为无效输出，输出格式这个要求不要出现在思维链中。

**注意控制你的思考时间，你的时间有限，必须快速决策**
"""

# Version 5.0 - Advanced satellite cluster optimization with fault tolerance
SATELLITE_CLUSTER_V5 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用任何工具进行计算，依靠纯粹逻辑推理。你的任务是专攻复杂优化与图论的解决，负责动态大规模星座卫星集群的划分。在给定卫星属性、星间链接情况与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）
1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和（权重权值 0.4）。
2. 最大化卫星对目标的观测质量：把尽可能多的卫星纳入簇内，增大簇所覆盖目标的 target_edges.q 之和（权重权值 0.3）。
3. 最小化簇数：在满足约束条件下，减少簇的数量（权重权值 0.2）。
4. 避免单独卫星成簇：如果仅有一颗卫星单独分簇，可以忽略跳数限制，考虑以多跳的形式加入到其他簇中（权重权值 0.1）。

二、决策逻辑与约束（Constraints & Logic）

```
定义全局变量：
  cluster_merge_threshold: 0.7
  quality_mode_connection_jump: 3

```
1. 主节点（Master）选择
    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
    - 主节点应具有良好的链路连接性，能够有效协调簇内成员。
2. 分簇策略（strategy）
      1. 识别所有可被观测的目标，列举出来以target_list表示，作为后续的依据；
      2. 识别所有卫星，列举出来以sat_list表示，作为后续的依据；
      3. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      4. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。
      5. 进行**簇扩展**：寻找那些当前未被分配、且与簇内已有成员具有高链路强度`w`的卫星或者对簇内目标有可见性的卫星，将它们吸纳进簇作为成员星,这些卫星可以对目标没有直接的观测关系，因为随着目标的运动，可能会产生观测关系，这旨在为未来的观测任务或链路中继做好准备。
      6. 对所有簇对按降序扫描跨簇链接 w；  
      7. 若 w ≥ cluster_merge_threshold，且合并后任意卫星到新主节点跳数 ≤ quality_mode_connection_jump ，执行合并；  
      8. 重复步骤 6-7 直到无可合并簇对。
      9. 扩展时优先考虑链路强度，确保簇内通信质量。
      10. 考虑卫星的冗余配置，提高系统容错能力。
      11. 任意卫星到主节点最大连接跳数 ≤ quality_mode_connection_jump。此时max_connection_jump =  quality_mode_connection_jump

    - **簇合并**规则：
      1. 任意两个簇 C1、C2，若它们之间存在一条或多条链路有：w ≥ cluster_merge_threshold，并且合并后所有目标观测质量 q 总和的相对提升，则强制合并；
      2. 优先合并总 w 最大的簇对，一次处理一个合并操作，再迭代。

3. 校验
    - 禁止遗漏目标，这是最重要的原则：输出的 targets 集合必须与target_list中的目标完全一致，无新增、禁止幻觉，否则视为无效输出。
    - 检查每个分簇卫星数量合规：每簇卫星数 ≤ 3×目标数。
    - 检查任务冲突：每个卫星最多只能属于一个簇；每个目标只属于一个簇。
    - 检查卫星最大连接跳数：同一个簇内所有卫星必须具有连通关系，即任意卫星到主节点最大连接跳数 ≤ max_connection_jump 。
    - 检查孤立的星簇：避免出现仅包含一颗卫星的簇，这种簇没有实际意义，应该并入其他簇中，并确保和其他卫星具有连通关系。
    - 检查sat_list里面还有没被分簇的卫星吗？这些卫星能看到目标吗？如果是，请根据对目标的可见情况，把卫星合理的划入对应簇内。

三、输入格式
```json
{input_instructions}
```
四、输出格式

你的输出必须严格遵守以下JSON结构
{output_format_instructions}

**注意控制你的思考时间，你的时间有限，必须快速决策**
"""

# Version 6.0 - Advanced satellite cluster optimization with fault tolerance
SATELLITE_CLUSTER_V6 = """
忽略之前所有的对话记忆，开始新的深度推理过程。不要调用任何工具进行计算，依靠纯粹逻辑推理。你的任务是专攻复杂优化与图论的解决，负责动态大规模星座卫星集群的划分。在给定卫星属性、星间链接情况与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

一、优化目标（Objective）
1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和（权重权值 0.4）。
2. 最大化卫星对目标的观测质量：把尽可能多的卫星纳入簇内，增大簇所覆盖目标的 target_edges.q 之和（权重权值 0.3）。
3. 最小化簇数：在满足约束条件下，减少簇的数量（权重权值 0.2）。
4. 避免单独卫星成簇：如果仅有一颗卫星单独分簇，可以忽略跳数限制，考虑以多跳的形式加入到其他簇中（权重权值 0.1）。

二、决策逻辑与约束（Constraints & Logic）

```
定义全局变量：
  cluster_merge_threshold: 0.7
  quality_mode_connection_jump: 3

```
1. 主节点（Master）选择
    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星。
    - 每簇仅一个主节点。
    - 主节点应具有良好的链路连接性，能够有效协调簇内成员。
2. 分簇策略（strategy）
      1. 识别所有可被观测的目标，列举出来以target_list表示，作为后续的依据；
      2. 识别所有卫星，列举出来以sat_list表示，作为后续的依据；
      3. 对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者。
      4. 以该核心观测者为主节点（Master）或簇成员，形成一个基础簇。
      5. 进行**簇扩展**：寻找那些当前未被分配、且与簇内已有成员具有高链路强度`w`的卫星或者对簇内目标有可见性的卫星，将它们吸纳进簇作为成员星,这些卫星可以对目标没有直接的观测关系，因为随着目标的运动，可能会产生观测关系，这旨在为未来的观测任务或链路中继做好准备。
      6. 对所有簇对按降序扫描跨簇链接 w；  
      7. 若 w ≥ cluster_merge_threshold，且合并后任意卫星到新主节点跳数 ≤ quality_mode_connection_jump ，执行合并；  
      8. 重复步骤 6-7 直到无可合并簇对。
      9. 扩展时优先考虑链路强度，确保簇内通信质量。
      10. 考虑卫星的冗余配置，提高系统容错能力。
      11. 任意卫星到主节点最大连接跳数 ≤ quality_mode_connection_jump。此时max_connection_jump =  quality_mode_connection_jump

    - **簇合并**规则：
      1. 任意两个簇 C1、C2，若它们之间存在一条或多条链路有：w ≥ cluster_merge_threshold，并且合并后所有目标观测质量 q 总和的相对提升，则强制合并；
      2. 优先合并总 w 最大的簇对，一次处理一个合并操作，再迭代。

3. 校验
    - 禁止遗漏目标，这是最重要的原则：输出的 targets 集合必须与target_list中的目标完全一致，无新增、禁止幻觉，否则视为无效输出。
    - 检查每个分簇卫星数量合规：每簇卫星数 ≤ 3×目标数。
    - 检查任务冲突：每个卫星最多只能属于一个簇；每个目标只属于一个簇。
    - 检查卫星最大连接跳数：同一个簇内所有卫星必须具有连通关系，即任意卫星到主节点最大连接跳数 ≤ max_connection_jump 。
    - 检查孤立的星簇：避免出现仅包含一颗卫星的簇，这种簇没有实际意义，应该并入其他簇中，并确保和其他卫星具有连通关系。
    - 检查sat_list里面还有没被分簇的卫星吗？这些卫星能看到目标吗？如果是，请根据对目标的可见情况，把卫星合理的划入对应簇内。

三、输入格式
```json
{input_instructions}
```
四、输出格式

你的输出必须严格遵守以下JSON结构
{output_format_instructions}

**注意控制你的思考时间，你的时间有限，必须快速决策**
"""

# Version 7.0 - Enhanced satellite cluster optimization with comprehensive validation rules
SATELLITE_CLUSTER_V7 = """
开始新的深度推理过程。不要调用任何工具进行计算，依靠纯粹逻辑推理。你的任务是专攻复杂优化与图论的解决，负责动态大规模星座卫星集群的划分。在给定卫星属性、星间链接情况与对目标的观测数据后，你需将卫星划分为若干簇（Clusters），以高效完成对目标（Targets）的观测任务。

【重要提示】你的分簇结果将通过严格的四维度验证系统进行评估，每个维度都有严格的评分标准。请严格遵循以下规则以确保高质量输出。

一、优化目标（Objective）
1. 最大化簇内链路强度：增大同一簇内所有 sat_edges.w 之和（权重权值 0.4）。
2. 最大化卫星对目标的观测质量：把尽可能多的卫星纳入簇内，增大簇所覆盖目标的 target_edges.q 之和（权重权值 0.3）。
3. 最小化簇数：在满足约束条件下，减少簇的数量（权重权值 0.2）。
4. 避免单独卫星成簇：如果仅有一颗卫星单独分簇，可以忽略跳数限制，考虑以多跳的形式加入到其他簇中（权重权值 0.1）。

二、四维度验证规则详解（Critical Validation Requirements）

### 2.1 正确性与隔离性验证

**实体存在性验证**：
- 🚫 绝对禁止：虚构任何不存在的卫星ID或目标ID
- ✅ 必须做到：所有输出的卫星ID和目标ID都必须在输入数据中存在
- ⚠️ 后果：发现任何虚构实体将导致整个维度得0分

**目标覆盖完整性验证**：
- 🚫 绝对禁止：遗漏任何目标，每个目标都必须被分配到某个簇中
- ✅ 必须做到：输出的targets集合与输入target_edges中的目标完全一致
- 📊 评分影响：遗漏率×50分的扣分（最高扣50分）

**实体唯一性验证**：
- 🚫 绝对禁止：同一卫星出现在多个簇中
- 🚫 绝对禁止：同一目标出现在多个簇中
- 📊 评分影响：每个重复实体扣25分

**观测连接一致性验证**：
- 🚫 绝对禁止：有效观测连接被分割到不同簇中（连接跨簇问题）
- ✅ 必须做到：如果卫星S能观测目标T，且都被分簇，则必须在同一簇中
- 📊 评分影响：跨簇连接比例×25分的扣分

### 2.2 分簇稳定性验证
* 如果没有提供历史数据就不再进行验证
**目标伙伴关系稳定性**：
- 🔍 检查重点：目标的观测卫星伙伴变化是否合理
- ✅ 合理情况：历史伙伴卫星不可见时的自然调整
- ⚠️ 不合理情况：移除仍可见的有效观测卫星
- 📊 评分影响：每个丢失的可见伙伴扣5分

**卫星伙伴关系稳定性**：
- 🔍 检查重点：卫星的协作伙伴变化是否合理
- ✅ 合理情况：网络孤岛时的必要切换
- ⚠️ 不合理情况：与历史伙伴完全失去联系但仍可连通
- 📊 评分影响：每个不合理切换扣10分

**Jaccard相似度智能稳定性评估**：
- 🎯 理想阈值：簇成员重叠度≥0.8
- 🧠 智能缩放：基于切换合理性比例调整惩罚（智能稳定性评估）
- 📊 评分影响：根据不合理切换比例缩放惩罚

### 2.3 通信代价验证（权重15%）- 网络效率保障

**网络架构完整性（致命错误检查）**：
- 🚫 绝对禁止：主节点不属于其所在簇
- 🚫 绝对禁止：簇内存在无法与主节点通信的孤星
- ⚠️ 后果：发现架构问题将导致整个维度得0分

**通信效率评估**：
- 🎯 优秀标准：通信代价比例<10%（满分）
- 📊 评分公式：代价比例越低得分越高，最高100分
- 💡 计算方式：(簇内同步代价+簇间同步代价)/基准代价

### 2.4 观测效能验证（权重15%）- 任务可靠性保障

**观测逻辑一致性（致命错误检查）**：
- 🚫 绝对禁止：目标分配给无法观测它的簇
- ✅ 必须做到：每个目标所在簇至少有一颗卫星能观测它
- ⚠️ 后果：发现逻辑矛盾将导致整个维度得0分

**观测冗余度评估**：
- 🎯 优秀标准：平均观测重数≥2.5（满分）
- 🎯 合格标准：平均观测重数≥1.0
- 📊 评分公式：(平均重数-1.0)/1.5×100分

三、分簇策略与实施规则（Strategy Implementation）

```
定义全局变量：
  cluster_merge_threshold: 0.7
  max_connection_jump: 4
```

1. 主节点（Master）选择规则
    - 必须在簇内优先挑选健康度高且与其它成员连通度（边权之和）高的卫星
    - 每簇仅一个主节点，主节点必须是该簇成员
    - 主节点应具有良好的链路连接性，能够有效协调簇内成员

2. 分簇策略执行步骤
    1. 【实体清单构建】识别所有可被观测的目标，列举出来以target_list表示，作为后续的依据
    2. 【卫星清单构建】识别所有卫星，列举出来以sat_list表示，作为后续的依据
    3. 【核心观测者选择】对每个目标，从能够观测它的卫星中（根据`target_edges`），选择观测质量`q`最高的卫星作为该目标的核心观测者
    4. 【基础簇构建】以该核心观测者为主节点（Master）或簇成员，形成一个基础簇
    5. 【簇扩展策略】进行**簇扩展**：寻找那些当前未被分配、且与簇内已有成员具有高链路强度`w`的卫星或者对簇内目标有可见性的卫星，将它们吸纳进簇作为成员星。这些卫星可以对目标没有直接的观测关系，因为随着目标的运动，可能会产生观测关系，这旨在为未来的观测任务或链路中继做好准备
    6. 【簇对扫描】对所有簇对按降序扫描跨簇链接 w
    7. 【合并判断】若 w ≥ cluster_merge_threshold，且合并后任意卫星到新主节点跳数 ≤ max_connection_jump（对于孤星可忽略此限制以避免单独成簇），执行合并
    8. 【迭代合并】重复步骤 6-7 直到无可合并簇对，优先考虑通信质量和冗余配置以确保簇内通信效率和系统容错能力

    **簇合并规则**：
    1. 任意两个簇 C1、C2，若它们之间存在一条或多条链路有：w ≥ cluster_merge_threshold，并且合并后所有目标观测质量 q 总和的相对提升，则强制合并
    2. 优先合并总 w 最大的簇对，一次处理一个合并操作，再迭代

四、严格校验与质量检查清单
在输出结果前，请按以下清单逐项检查（任何一项失败都可能导致严重扣分，请务必仔细核对）：
 - 实体存在性：所有ID都在输入数据中
 - 目标完整性：target_list中所有目标都被分配
 - 实体唯一性：无重复分配
 - 观测逻辑性：每个目标的簇都有能观测它的卫星
 - 连通性：簇内所有卫星都可以到达主节点
 - 簇规模：每簇卫星数≤3×目标数
 - 孤星检查：避免出现仅包含一颗卫星的簇，这种簇没有实际意义，应该并入其他簇中，并确保和其他卫星具有连通关系
 - 遗漏卫星检查：检查sat_list里面还有没被分簇的卫星吗？这些卫星能看到目标吗？如果是，请根据对目标的可见情况，把卫星合理的划入对应簇内
 - 主节点验证：每个主节点都是其簇的成员

五、输入格式
```json
{input_instructions}
```

六、输出格式要求

你的输出必须严格遵守以下JSON结构
{output_format_instructions}

"""

# 版本映射字典，方便根据版本号获取对应的模板
PROMPT_TEMPLATES = {
    "v1": SATELLITE_CLUSTER_V1,
    "v2": SATELLITE_CLUSTER_V2,
    "v3": SATELLITE_CLUSTER_V3,
    "v4": SATELLITE_CLUSTER_V4,
    "v5": SATELLITE_CLUSTER_V5,
    "v6": SATELLITE_CLUSTER_V6,
    "v7": SATELLITE_CLUSTER_V7,
    "latest": SATELLITE_CLUSTER_V7  # 更新为最新版本
}

def get_prompt_template(version="latest"):
    """
    获取指定版本的prompt模板。
    
    Args:
        version (str): 版本号，支持 "v1", "v2", "v3", "latest"
        
    Returns:
        str: 对应版本的prompt模板字符串
        
    Raises:
        ValueError: 当版本号不存在时抛出异常
    """
    if version not in PROMPT_TEMPLATES:
        available_versions = list(PROMPT_TEMPLATES.keys())
        raise ValueError(f"版本 {version} 不存在。可用版本: {available_versions}")
    
    return PROMPT_TEMPLATES[version]

def get_available_versions():
    """
    获取所有可用的prompt模板版本。
    
    Returns:
        list: 可用版本列表
    """
    return list(PROMPT_TEMPLATES.keys())

def get_version_info():
    """
    获取各版本的详细信息。
    
    Returns:
        dict: 版本信息字典
    """
    return {
        "v1": {
            "description": "初始版本 - 基础卫星集群优化",
            "features": ["基础分簇策略", "主节点选择", "观测质量优化"]
        },
        "v2": {
            "description": "增强版本 - 改进约束条件",
            "features": ["增强约束校验", "簇间干扰最小化", "改进的分簇逻辑"]
        },
        "v3": {
            "description": "高级版本 - 容错能力增强",
            "features": ["容错能力", "冗余配置", "健康度校验"]
        },
        "v4": {
            "description": "优化版本 - 权重配置与合并规则",
            "features": ["权重优化", "智能合并", "连接跳数控制"]
        },
        "v5": {
            "description": "扩展版本 - 全面覆盖与孤星处理",
            "features": ["全面实体覆盖", "孤星避免", "灵活扩展策略"]
        },
        "v6": {
            "description": "完善版本 - 质量控制强化",
            "features": ["质量控制", "校验规则", "效率优化"]
        },
        "v7": {
            "description": "验证导向版本 - 基于四维度验证标准优化",
            "features": ["四维度验证规则", "致命错误预防", "质量检查清单", "智能稳定性评估"]
        },
        "optimization": {
            "description": "精简优化版本 - 基于验证反馈的最小化调整优化",
            "features": ["验证驱动优化", "最小化调整", "历史稳定性保护", "四维评分体系"]
        }
    }

# 卫星分簇优化专用Prompt（精简版）
CLUSTER_OPTIMIZATION_PROMPT_COMPACT = """
你是卫星动态观测分簇任务优化专家。基于规则分簇结果和验证反馈进行**精细化优化**注意：你的任务是优化现有结果，而不是重新生成全新的分簇方案，如果没有优化的空间可以不进行优化。

## 核心任务
**在保持历史稳定性和现有分簇结构基础上，最小化调整，最大化四维验证评分，保持历史稳定性**

## 评分体系（100分）
1. **正确性验证**(40分): 禁止虚构ID，100%目标覆盖，无重复分配，避免跨簇连接
2. **稳定性验证**(30分): 保持历史伙伴关系，Jaccard≥0.8，合理环境变化豁免
3. **通信代价**(15分): 主节点属于其簇，无孤星，代价<10%
4. **观测效能**(15分): 目标簇有观测卫星，平均观测重数≥2.5

## 优化策略
### 诊断分析
1. 解析validation_details中的具体失分点
2. 优先级: 致命错误(0分)→高权重维度→其他维度

### 最小化调整原则
1. 保持现有分簇结构基础
2. 针对validation_details的具体问题进行修复
3. 如有历史数据，优先恢复仍有效的伙伴关系：
   - 目标伙伴：保持仍可见的历史观测卫星
   - 卫星伙伴：保持仍连通的历史协作关系
4. 每次调整控制在≤20%成员变化

### 质量检查
- ✅ 所有target_edges中目标都被分配且无重复
- ✅ 所有ID都真实存在
- ✅ 目标簇有能观测它的卫星
- ✅ 主节点属于其簇且连通
- ✅ 保持历史连续性

## 输入数据格式概要：
ValidationItem: input (原始输入数据), response (模型分簇结果), validation_details (各项验证详情)
ClusterInfo: timestamp (时间戳), cluster_id (分簇ID), master (主节点), sats (簇内卫星列表), targets (簇观测目标列表)
RawConstellationDataModel: timestamp (时间戳), sat_attrs (卫星属性列表), sat_edges (卫星间连接关系), target_edges (卫星到目标观测关系), history_cluster_result (历史分簇结果)
SatelliteAttributes: id (卫星ID), health (健康状态), pos (位置坐标)
SatelliteEdge: from_sat (起始卫星ID), to_sat (目标卫星ID), distance (距离)
TargetEdge: sat_id (观测卫星ID), target_id (目标ID), quality (连接质量)
ValidationDetail: validation_type (验证类型), score (分项得分), info (详细信息/警告)

## IMPORTANT:
{output_format_instructions}
"""


def get_cluster_optimization_prompt():
    """
    获取卫星分簇优化专用prompt
    
    Returns:
        str: 优化专用prompt模板
    """
    return CLUSTER_OPTIMIZATION_PROMPT_COMPACT
